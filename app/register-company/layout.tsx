import AppLayoutWrapper from '@/components/app-layout-wrapper';

export default function RegisterCompanyLayout({ children }: { children: React.ReactNode }) {
  return <AppLayoutWrapper>{children}</AppLayoutWrapper>;
}
