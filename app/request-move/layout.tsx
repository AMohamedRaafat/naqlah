import PublicLayoutWrapper from '@/components/layout/public-layout-wrapper';

export default function OrderMoveLayout({ children }: { children: React.ReactNode }) {
  return <PublicLayoutWrapper>{children}</PublicLayoutWrapper>;
}

